# PKM 사이트 디자인 방향

> 대상: `https://dkwlsdl3.github.io/pkm/` (Quartz v4.5.2)
> 작성: 2026-08-07 · 상태: **초안 — 확정 전**

---

## 1. 이 사이트는 무엇인가

기술 노트를 쌓아 두고 **몇 달 뒤의 내가 다시 읽는** 개인 지식 베이스다. 방문자를 설득하거나 상품을 파는 사이트가 아니다. 그래서 판단 기준은 하나다.

**"긴 기술 문서를 오래 읽어도 지치지 않는가."**

히어로 이미지, 애니메이션, 브랜드 연출은 이 기준에 기여하지 않으면 넣지 않는다.

---

## 2. 원칙 4가지

1. **읽기가 최우선.** 본문 폭·행간·글자 크기를 먼저 정하고 나머지를 거기에 맞춘다.
2. **색은 의미를 가질 때만.** 장식용 색을 쓰지 않는다. 링크, 코드, 경고처럼 구분이 필요한 곳에만 쓴다.
3. **위계는 크기와 여백으로.** 선과 상자를 늘리는 대신 여백 차이로 구조를 드러낸다.
4. **한글이 1급 시민.** 지금 본문 폰트(Source Sans Pro)에는 한글 글리프가 없어 시스템 폰트로 대체되고 있다. 영문 기준으로 잡은 행간·자간이 한글에서 무너진다.

---

## 3. 참고처와 가져올 것

실제로 열어 보고 판단했다.

### Apple Developer Documentation (HIG)

`developer.apple.com/design/human-interface-guidelines`

지금 우리 사이트와 **구조가 같다** — 좌측 네비 / 본문 / 우측 목차 3단.

- **가져올 것**: 도입 문단을 본문보다 크게 써서 페이지 성격을 먼저 알리는 방식, 흰 배경 + 회색 계층만으로 만드는 위계, 좌측 네비의 얇은 구분과 낮은 대비, 넉넉한 본문 상단 여백
- **버릴 것**: 컬러 일러스트 카드(우리 노트에는 이미지가 거의 없다)

### Maggie Appleton — The Garden

`maggieappleton.com/garden`

개인 지식 정원의 대표 사례. 우리와 목적이 가장 가깝다.

- **가져올 것**: 목록을 표가 아니라 **카드 그리드**로 두고 제목·한 줄 요약·메타(날짜·태그)를 묶는 방식, 상단 태그 필터, 따뜻한 오프화이트 배경, 큰 제목과 본문의 과감한 크기 차이
- **버릴 것**: 손그림 일러스트와 성장 단계(새싹/묘목) 은유 — 우리 노트 성격과 안 맞는다

### Airbnb Tech Blog — 참고하지 않음

`airbnb.tech/blog`

물어보셔서 열어 봤는데, 큰 일러스트 히어로 + 카드 중심이라 **이미지가 있어야 성립하는 디자인**이다. 텍스트만 있는 우리 노트에 적용하면 빈 카드만 남는다.

---

## 4. 하지 않을 것

- 특정 회사의 **로고·아이콘·브랜드 색을 그대로 쓰지 않는다.** 원칙(여백 운용, 타이포 위계)은 참고하되 결과물이 그 회사 사이트로 오인되면 안 된다.
- **SF Pro를 쓰지 않는다.** Apple 플랫폼 전용 라이선스라 웹 배포에 부적합하다. 대신 같은 계열의 인상을 내는 Pretendard를 쓴다.
- 스크롤 애니메이션, 패럴랙스, 등장 효과 — 재방문 시 매번 기다려야 한다.

---

## 5. 스펙

### 5.1 타이포그래피

| 용도      | 폰트                                               | 근거                                                                                                          |
| --------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 본문·제목 | **Pretendard Variable**                            | 한글은 본고딕 계열, 영문은 Inter 기반으로 다듬어져 한영 혼용 문장에서 크기·무게가 어긋나지 않는다. 9단계 굵기 |
| 코드      | **JetBrains Mono** (대안: 현행 IBM Plex Mono 유지) | 0/O, 1/l 구분이 뚜렷                                                                                          |

로딩은 jsDelivr **동적 서브셋**을 쓴다 — 페이지에 실제로 쓰인 글자만 내려받아 한글 웹폰트의 용량 부담을 줄인다.

```
https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/variable/pretendardvariable-dynamic-subset.css
```

**크기 스케일** (본문 16px 기준, 1.25배수)

| 요소            | 크기    | 무게 | 행간    |
| --------------- | ------- | ---- | ------- |
| 페이지 제목(h1) | 2.5rem  | 700  | 1.25    |
| 도입 문단       | 1.25rem | 400  | 1.7     |
| 본문            | 1rem    | 400  | **1.8** |
| h2              | 1.6rem  | 600  | 1.35    |
| h3              | 1.25rem | 600  | 1.4     |
| 메타(날짜·태그) | 0.85rem | 400  | 1.5     |

한글은 영문보다 글자당 밀도가 높아 행간 1.8이 필요하다. 현재 기본값(1.6 내외)은 빽빽하다.

### 5.2 색

**라이트 모드** — 순백 대신 아주 옅은 온기를 넣어 장시간 읽을 때 눈부심을 줄인다.

| 토큰      | 값                       | 용도                 |
| --------- | ------------------------ | -------------------- |
| light     | `#fbfaf9`                | 페이지 배경          |
| lightgray | `#e8e6e3`                | 구분선, 코드 배경    |
| gray      | `#a8a29e`                | 비활성, 플레이스홀더 |
| darkgray  | `#57534e`                | 메타 텍스트          |
| dark      | `#1c1917`                | 본문                 |
| secondary | `#2f5d62`                | 링크·강조 (딥 틸)    |
| tertiary  | `#7a9e9f`                | 호버·그래프 노드     |
| highlight | `rgba(47, 93, 98, 0.08)` | 내부 링크 배경       |

**다크 모드** — 순흑(`#000`)은 대비가 과해 눈이 피로하다.

| 토큰      | 값                          |
| --------- | --------------------------- |
| light     | `#1a1a1c`                   |
| lightgray | `#2f2f33`                   |
| gray      | `#6b6b70`                   |
| darkgray  | `#c9c7c4`                   |
| dark      | `#eeedeb`                   |
| secondary | `#8fb8bb`                   |
| tertiary  | `#7a9e9f`                   |
| highlight | `rgba(143, 184, 187, 0.12)` |

### 5.3 레이아웃

- 본문 최대 폭 **68ch** (한글 기준 한 줄 34~38자). 현재는 더 넓어 시선 이동이 길다.
- 본문 상단 여백을 늘려 제목이 화면 위에 붙지 않게 한다.
- 좌측 탐색기: 폴더명 자간을 넓히고 크기를 줄여 본문과 경쟁하지 않게 한다.
- 우측 사이드바: 이미 스크롤 가능하게 고쳤다. 그래프는 접을 수 있게 두고 최근 노트를 위로 올린다.

### 5.4 목록 페이지 (`/all-notes`, 폴더·태그 페이지)

지금은 `날짜 · 제목 · 태그`가 한 줄에 늘어서 스캔이 어렵다.

- 제목을 첫 줄에 크게, 날짜·태그는 그 아래 작은 메타 줄로 내린다.
- 항목 사이 구분선 대신 **여백 + 호버 배경**으로 나눈다.
- 날짜는 `2026년 8월 07일` 대신 `2026.08.07`로 짧게 — 목록에서는 정렬감이 중요하다.

---

## 6. 구현 매핑

| 작업                                                            | 파일                         | 업스트림 충돌                  |
| --------------------------------------------------------------- | ---------------------------- | ------------------------------ |
| 색 팔레트, 폰트 지정                                            | `quartz.config.ts`           | 없음                           |
| 폰트 로딩(동적 서브셋), 타이포 스케일, 본문 폭, 목록 카드, 여백 | `quartz/styles/custom.scss`  | 없음                           |
| 컴포넌트 배치, 사이드바 순서                                    | `quartz.layout.ts`           | 없음                           |
| 날짜 포맷(`2026.08.07`)                                         | `quartz/components/Date.tsx` | 있음(이미 타임존으로 1줄 수정) |

**핵심**: 대부분 `custom.scss`와 설정 파일로 끝난다. Quartz 코어를 건드리는 건 날짜 포맷 하나뿐이라 업스트림 업데이트 부담이 거의 없다.

---

## 7. 적용 순서

1. 폰트 교체 + 타이포 스케일 + 본문 폭 — **체감 변화의 8할**
2. 색 팔레트 교체 (라이트/다크)
3. 목록 페이지 카드화
4. 사이드바 정리
5. 다크 모드 대비 검증(WCAG AA: 본문 4.5:1 이상)

각 단계는 로컬 빌드로 스크린샷을 찍어 확인한 뒤 배포한다.
